{
  "name": "@f/compose-reducers",
  "description": "Compose two or more reducers",
  "repository": "git://github.com/micro-js/compose-reducers.git",
  "version": "1.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/to-array": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
